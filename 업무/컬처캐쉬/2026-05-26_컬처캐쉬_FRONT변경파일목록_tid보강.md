---
project: 컬처캐쉬
step: FRONT TID 보강
created: 2026-05-26
updated: 2026-05-26
status: done
tags:
  - 업무
  - 핑거페이
  - 컬처캐쉬
  - FRONT
  - STEP1
up: "[[_INDEX]]"
related: "[[_LATEST]]"
---

# 컬처캐쉬 — FRONT TID 보강 (2026-05-26)

## 배경

- 사용자 보고: FRONT → BLD 송신 시 `tid` 누락.
- 추적: `/payment/v1/approval` → `PayService.approval()` 의 `selectTransRequest(nonce)` 가 `TBTR_REQ` row SELECT → `transMap` 으로 전달 → `PayMethodService.doMakeBldSendApprovalData()` 가 `transMap.tid` 를 BLD 송신 데이터에 박음.
- 원인: 컬처 흐름은 PG 콜백 단계에서 `updateTransRequest` 호출이 없어 `TBTR_REQ.TID` 가 빈 상태. → `transMap.tid = null`.
- 비교: Card/Vacnt 는 PG 결과 콜백 시 `saveTransReq` / `updateTransRequest` 가 TID 갱신해줌. 컬처만 누락.

## 적용 (Fix A + B 둘 다 — 임시 다층 방어)

### Fix B — 콜백에서 TBTR_REQ.TID 보강 (근본 해결, 다른 PG 흐름 모방)

| 파일 | 절대경로 | 변경 |
|---|---|---|
| MerchantMapper.java | `C:\Users\finger\Downloads\01. 작업\07. 컬처캐시 연동\front\src\main\java\solpay\wiezon\com\mapper\MerchantMapper.java` | `updateTransRequestTidForCulture(Map)` 메서드 추가 |
| MerchantMapper.xml | `C:\Users\finger\Downloads\01. 작업\07. 컬처캐시 연동\front\src\main\resources\mapper\MerchantMapper.xml` | `<update id="updateTransRequestTidForCulture">` 신설 — `UPDATE TBTR_REQ SET TID=#{tid}, PM_CD=#{pmCd}, SPM_CD=#{spmCd} WHERE NONCE=#{nonce}` |
| CultureReturnController.java | `C:\Users\finger\Downloads\01. 작업\07. 컬처캐시 연동\front\src\main\java\solpay\wiezon\com\controller\CultureReturnController.java` | `cultureReturn()` 끝에서 `effectiveTid` 가 정해진 후 `updateTransRequestTidForCulture` 호출 (nonce/tid/pmCd=32/spmCd=07) |

- 카드 전용 updateTransRequest (CARD_NO/QUOTA_MON/CRCT_* 등) 와 분리. 컬처는 TID/PM_CD/SPM_CD 만 갱신 필요.
- 실패는 warn 로그만 — 보강 실패해도 Fix A 가 받쳐줌.

### Fix A — Service 단 보강 (방어 다층)

| 파일 | 절대경로 | 변경 |
|---|---|---|
| CultureCashService.java | `C:\Users\finger\Downloads\01. 작업\07. 컬처캐시 연동\front\src\main\java\solpay\wiezon\com\payMethod\service\CultureCashService.java` | `doMakeBldSendApprovalData(transMap, reqMap)` 오버라이드 — `transMap.tid` 가 비고 `reqMap.tid` (cultureReturn.html hidden) 가 있으면 `transMap.tid` 에 보강 후 `super` 호출 |

- CardService/VacntService 오버라이드 패턴 차용.
- Fix B 가 누락/실패해도 BLD 송신 데이터 tid 가 박힘.

## 검증 시나리오

1. FRONT 재기동 (mapper.xml 변경 반영).
2. sample.html → 「문화상품권 결제」 → MOCK 흐름 → /cultureReturn.do 콜백 결과 페이지.
3. 「🚀 BLD 승인 처리 진행」 클릭 → `/payment/v1/approval`.
4. BLD 로그에서 수신 데이터의 `tid` 가 채워진 30자 문자열인지 확인 (이전: null/빈값).
5. `SELECT TID, NONCE, PM_CD, SPM_CD FROM TBTR_REQ WHERE NONCE='<해당 nonce>'` 로 TID 컬럼 채워졌는지 확인.

## LIVE 전환 시 정리 (Phase 3)

- `// STEP1 임시` 주석 부위 검색.
- Fix B 의 콜백 UPDATE 는 LIVE 흐름에서도 그대로 유효 — 컬쳐랜드 실제 RetVal 받기 전후 어느 시점에서 호출할지만 재배치.
- Fix A 의 fallback 은 LIVE 흐름에서 transMap.tid 가 정상 채워지면 dead code 가 되지만, 안전망이라 남겨도 무방. 정리 시 제거.
- 근본 해결: `CultureCashService.doStart()` 에서 TID 생성 타이밍을 doStart 이전으로 옮기거나, doStart 내부에서 `createTid` 호출 + `updateTransRequest` 호출.

## 영향 받지 않는 부분

- BLD 코드 (`PayCultureBean.java` 등) — 변경 없음.
- 다른 PG 흐름 (Card/Vacnt/CRCT) — 변경 없음 (신설 매퍼 메서드 호출 없음).
- `cultureReturn.html` form — 변경 없음 (이미 tid hidden 필드 존재).
